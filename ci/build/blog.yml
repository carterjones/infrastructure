platform: linux

image_resource:
  type: docker-image
  source:
    repository: carterjones/infra-builder

inputs:
  - name: repo

outputs:
  - name: ami

run:
  path: repo/boxes/build.sh
  args: [blog]
