platform: linux

image_resource:
  type: docker-image
  source:
    repository: carterjones/infra-builder

inputs:
  - name: repo
  - name: ami-in

outputs:
  - name: ami-out

params:
  ROLE:
  TIER:

run:
  path: repo/ci/test/ssh-login.sh
  args: [base]
