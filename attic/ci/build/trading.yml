platform: linux

image_resource:
  type: docker-image
  source:
    repository: carterjones/infra-builder

inputs:
  - name: repo-trading
  - name: repo-ci

outputs:
  - name: out

run:
  path: repo-ci/ci/build/trading.sh
